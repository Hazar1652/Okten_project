# Frontend placeholder

React app is intentionally not scaffolded yet.

Recommended next command:
- `npm create vite@latest . -- --template react-ts`

Then add frontend container to `infra/docker-compose.yml`.
